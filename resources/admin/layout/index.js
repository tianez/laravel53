'use strict'
const Layout = require('./layout')
const Header = require('./header')
const Footer = require('./footer')
var Temp = {
    Layout: Layout,
    Header: Header,
    Footer: Footer,
}
module.exports = Temp